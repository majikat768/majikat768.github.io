
/*
 * TODO: implement a find_next_cell function for cell class
 */

import sketch from './sketch.js';
import { p5ctx, setContext } from './const.js';

setContext(new p5(sketch));

// const init_nodes = function() {
//   for(let i = 0; i < n_nodes; i++) {
//         let x= Math.floor(Math.random() * (column_count));
//         let y= Math.floor(Math.random() * (row_count));
//     grid.cells[x][y].visit();
//     nodes.push(grid.cells[x][y]);
//   }
// }
